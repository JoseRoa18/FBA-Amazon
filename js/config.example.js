// js/config.example.js
// Copiá este archivo a "js/config.js" y pegá tus credenciales reales.
// config.js está en .gitignore así que nunca se sube a git.

window.FBA_CONFIG = {
    supabase: {
        url: '',      // Project URL de Supabase → Settings → API
        anonKey: '',  // anon public key de Supabase → Settings → API
    }
};
